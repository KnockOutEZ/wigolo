/**
 * Public surface of `src/search/` for the companion split (`wigolo/search-tokens`).
 *
 * Token counting only — the extracted layer budgets its result text with the same
 * counter core budgets tool output with.
 */
export { countTokens } from './tokens.js';
