/* eslint-disable */
/*
 * SEA FOOTER — closes the wrapper `banner.js` opens. See that file for what the three
 * arguments are and why they are passed as parameters rather than assigned.
 *
 * Kept as its own file, and appended with esbuild's `--footer:js`, so that the open and the
 * close are visibly a pair. A wrapper closed inside the build script instead would be one
 * `)` living a hundred lines away from its `(` in a different language.
 */
})(__wigoloRequire, __wigoloAnchor, __wigoloAnchorDir);
